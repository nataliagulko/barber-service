{{ticket-steps class=""}}
<div class="grid">
    <div class="row">
        <div class="col-md-5">
            {{ticket-info}}
        </div>
        <div class="col-md-7">
            {{ticket-client}}
            {{ticket-master class = "hidden"}}
            {{ticket-services class = "hidden"}}
            {{ticket-date class = "hidden"}}
            {{ticket-time class = "hidden"}}
        </div>
    </div>
</div>
{{outlet}}