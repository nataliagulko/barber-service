{{ticket-steps class=""}}
<div class="grid">
    <div class="row">
        <div class="col-md-5">
            {{ticket-info}}
        </div>
        <div class="col-md-7">
            {{ticket-master masters=model.masters}}
            {{ticket-services}}
            {{ticket-date}}
            {{ticket-time}}
            {{ticket-client}}
        </div>
    </div>
</div>