{{ticket-steps class=""}}
<div class="grid">
    <div class="row">
        <div class="col-md-5">
            {{ticket-info}}
        </div>
        <div class="col-md-7">
            {{ticket-master}}
            {{ticket-services class = "hidden"}}
            {{ticket-date class = "hidden"}}
            {{ticket-time class = "hidden"}}
            {{ticket-client class = "hidden"}}
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <button type="button" class="btn btn-lg green pull-right" disabled>Записать</button>
        </div>
    </div>
</div>
{{outlet}}