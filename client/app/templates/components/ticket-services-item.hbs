<div class="mt-widget-1" {{action 'selectServiceItem' item-name item-time item-price}}>
    <div class="mt-body">
        <p class="mt-user-title">{{item-name}}</p>
        <div class="mt-stats no-space">
            <div class="btn-group btn-group-justified">
                <a href="javascript:;" class="btn font-blue no-space">{{item-time}}</a>
                <a href="javascript:;" class="btn font-blue">{{item-price}}</a>
            </div>
        </div>
    </div>
</div>

{{yield}}